// External resources configuration

// Font configuration
export const fontConfig = {
  inter: {
    url: 'https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;500;600;700;800;900&display=swap',
    weights: [100, 200, 300, 500, 600, 700, 800, 900]
  }
};

// Chart library configuration
export const chartConfig = {
  apexcharts: 'https://cdn.jsdelivr.net/npm/apexcharts'
};

// Icon configuration
export const iconConfig = {
  fontawesome: {
    url: 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js',
    config: {
      autoReplaceSvg: 'nest' // Options: 'nest', 'remove', 'replace'
    }
  }
};

// Tailwind configuration
export const tailwindConfig = {
  url: 'https://cdn.tailwindcss.com'
};